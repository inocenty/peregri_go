<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.peregri.peregrigo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="WyborTrasy"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="OknoGlowne" android:label="@string/app_name"></activity>
        <activity android:label="@string/app_name" android:name="PunktEdu"></activity>
        <activity android:label="@string/app_name" android:name="PunktSport"></activity>
        <activity android:label="@string/app_name" android:name="Podsumowanie"></activity>
        <activity android:label="@string/app_name" android:name="PunktZlaKolejnosc"></activity>
        <activity android:label="@string/app_name" android:name="ZlyPunkt"></activity>
    </application>

</manifest>